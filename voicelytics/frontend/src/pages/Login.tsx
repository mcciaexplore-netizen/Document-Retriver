import {useState,FormEvent} from 'react';
import {Activity,ArrowRight,ShieldCheck,Radio,GitBranch} from 'lucide-react';
import {useSession} from '../stores/session';
import {errorMessage} from '../services/api';
export function Login(){const {login}=useSession();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password);}catch(e){setError(errorMessage(e));}finally{setBusy(false);}}return <div className="login-page"><section className="login-story"><div className="brand"><span className="brand-mark"><Activity/></span>VOICELYTICS</div><div className="login-message"><div className="eyebrow">FROM THE FLOOR. TO THE FOREFRONT.</div><h1>Every report.<br/>A clearer picture.</h1><p>Connect the people, machines, and decisions that keep your factory moving.</p><div className="login-grid"><div><Radio/><strong>Report with context</strong><span>Voice evidence and written observations</span></div><div><GitBranch/><strong>Act with confidence</strong><span>Transparent rules and clear ownership</span></div><div><ShieldCheck/><strong>Close the loop</strong><span>Verified actions and a complete audit trail</span></div></div></div><small>Factory Floor Voice Assistant &amp; Operations Intelligence</small></section><section className="login-form"><form onSubmit={submit}><div className="eyebrow">YOUR OPERATIONS WORKSPACE</div><h2>Welcome back</h2><p className="muted">Sign in to see what’s happening on your floor.</p><label>Work email<input autoComplete="username" type="email" required placeholder="technician@voicelytics.local" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input autoComplete="current-password" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div role="alert" className="form-error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in to workspace'}<ArrowRight size={18}/></button><p className="login-note">Demo accounts use the password configured in <code>DEMO_PASSWORD</code> during setup. Contact your administrator for access.</p></form></section></div>;}
