# FastAPI backend for a separate Vercel frontend.
services:
  - type: web
    name: document-retriever-api
    runtime: docker
    plan: starter
    dockerfilePath: ./backend/Dockerfile.backend
    dockerContext: .
    healthCheckPath: /api/health
    disk:
      name: document-data
      mountPath: /app/storage
      sizeGB: 1
    envVars:
      - key: DATABASE_URL
        value: sqlite:////app/storage/mccia.db
      - key: STORAGE_PATH
        value: /app/storage
      - key: COOKIE_SECURE
        value: "true"
      - key: DEMO_SEED
        value: "false"
      - key: ALLOW_REGISTRATION
        value: "false"
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: CORS_ORIGINS
        sync: false
